// Auth hook — Pi login / logout / loading. Re-exported from the AuthContext so
// consumers can import it from the hooks/ surface like the other hooks.
export { useAuth } from '../context/AuthContext';
